# Copy to `.env` in this folder (do not commit real keys).

# Claude (Anthropic) — Generation → LLM when "Claude" is selected
ANTHROPIC_API_KEY=

# OpenAI API — Generation → LLM when "OpenAI" is selected
OPENAI_API_KEY=
